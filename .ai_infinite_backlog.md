# FBD — Infinite Production Loop Backlog

> Maintained by the production loop. Append-only `Done`; Pending never empties.
> Repo: https://github.com/FisiFla/FBD (private). macOS 13.2+, SwiftPM.

## Dear Future Me (last cycle: 2026-08-02, cycle 5)

Shipped this cycle:
- **HTTP routing layer extracted + fully tested (P2 — the last big test
  gap)**: pure `HTTPRouter` in FBDCore (auth 401, method 405, path 404,
  JSON body + per-action validation 400) returns typed `HTTPRoute`
  results; AppCore only executes routes against controllers. `HTTPJSON`
  moved to FBDCore (public) so both share it. Latent bug fixed in the
  move: routed `virtual create` now honors `isHDR` (was hardcoded false).
  AppCore lost ~230 lines of untested switch logic; behavior verified
  identical live (401/200/400/404/201 matrix + destroy-by-name).
- 21 new tests: auth (missing/wrong/Bearer), 405s, path syntax, hex+decimal
  display ids, all display actions incl. validation edges, XDR variants,
  virtual create/destroy validation incl. isHDR passthrough. Suite 115 ->
  136. (Test expectation fix: NSNumber 0/1 bridges to Bool for `muted`,
  matching pre-router behavior.)

Tests run: `swift test --disable-sandbox` (136/136), live HTTP matrix vs
app, virtual lifecycle.

Next focus: HotkeyController tap-failure surfacing (P1), a11y pass on the
NSPanel (P3), `fbdcli modes` current-mode marker + `--json` (P3).

## Production Readiness Scorecard

| Area | Status | Evidence |
|---|---|---|
| Builds (debug + release universal) | ✅ PASS | `swift build`, `make app` green locally + CI (macos-15, 1m54s) |
| Tests | ✅ PASS (97) | DDC wire/retry/caps, HTTP server round-trips, XDR presets, EDID parser, combined routing, settings, CLI parser |
| CI | ✅ PASS | GitHub Actions: build + test + make app + artifact; badge in README |
| Security: HTTP API | ✅ PASS | 127.0.0.1 only, token auth (fail-closed), CORS preflight, 1MB body cap, 405/401/503 |
| Security: URL scheme | ✅ PASS | `fbd://` input validated (UInt32/Double parse, display lookup, clamped writes) |
| Security: secrets/config | ✅ PASS | No hard-coded secrets; token auto-generated (SecRandom) |
| Security: file/input validation | ✅ PASS | EDID overrides structurally validated (size/header/checksum) at CLI, controller and persistence gates |
| Error handling/logging | ✅ PASS | os.Logger throughout; private APIs degrade to nil/false + log; no force unwraps, no try! |
| Distribution | ⚠️ PARTIAL | Ad-hoc signed; icon done; missing Developer ID + notarization + appcast (blocked: user cert/URL) |
| Docs | ✅ PASS | README (+feature matrix), ARCHITECTURE, ROADMAP, PRIVATE_APIS, AGENTS |
| Hardware verification | ⚠️ PARTIAL | DDC untested on real external monitor (blocked: hardware); CGVirtualDisplay untested on macOS 13–15 (blocked: hardware) |
| Dependency hygiene | ✅ PASS | Only Sparkle (binary, optional); vendored private-API headers documented |
| Perf | ⚠️ PARTIAL | Display model: ~20 @Published props (churn); HTTP handler bridges sync via semaphore (5s cap) |
| UX | ✅ PASS | Floating NSPanel (resizable, autosave, Esc/⌘W/× close, back nav), CLI parity |

## Done

- 2026-08-02: FBDCLIParser library + 17 CLI parser tests; app icon (.icns) + bundle wiring.
- 2026-08-02: EDIDValidation (size/header/checksum) enforced in apply/save/restore + CLI; 10 EDID tests.
- 2026-08-02: HTTPServer: 413 on oversized declared Content-Length, Expect: 100-continue ack; parseHead/sendResponse extraction; 2 raw-socket tests.
- 2026-08-02: VirtualDisplayRegistry (Settings-persisted, cross-process) as the authoritative isVirtual source; routed virtual destroy name resolution fix; 6 tests.
- 2026-08-02: HTTPRouter + HTTPJSON extracted to FBDCore; AppCore executes typed routes; isHDR passthrough fix; 21 router tests.
- 2026-08-02 (review v2): HTTP token auth + CORS; DDC tests run on all CI archs; VESA sub-value parsing; DisplayController surface narrowed; README feature matrix.
- 2026-08-02: NSPopover → floating NSPanel (window chrome, resize, autosave, navigation); Makefile stale-binary fix.
- 2026-08-01 (review v1): ColorProfileController crash fix; DDC retries + configurable settle; HTTP tests + 405/logging; CI pipeline; main-thread sleep fix; DDC instance dedup.
- 2026-08-01: Tier 5 (PiP, OSD, Night Shift, TV, HTTP API, Intents, Sparkle); Tier 4 (EDID, profiles, underscan, config protection); Tier 3 (virtual displays, layout); Tier 2 (XDR); Tier 1 (core DDC).
- 2026-07-31: RE of BetterDisplay → docs/betterdisplay-reverse-engineering.md + FBD scaffolding.

## Pending

### P0 (blocked — need user)
- [ ] Developer ID cert + notarization for release builds (env: certs/secrets).
- [ ] Sparkle appcast host URL (env: a URL; SUFeedURL is empty by design).
- [ ] Real-hardware DDC verification checklist run (env: Apple Silicon Mac + DDC monitor).

### P1 (correctness/robustness)
- [x] EDID override apply: validate file size (≤ 512 bytes, 128/256 multiple) before `defaults` write; reject garbage with a clear message. DONE (cycle 2: EDIDValidation + tests + CLI message).
- [x] HTTPServer: handle `Expect: 100-continue` and oversized `Content-Length` headers without buffering to the 1MB cap; verify keep-alive is safely `Connection: close` in all paths. DONE (cycle 3: 413 early reject + 100-continue ack + tests + live curl matrix).
- [ ] HotkeyController: surface event-tap failure (tap creation can silently fail on lockdown macOS); gate `interceptMediaKeys` failure with a one-time log + menu hint. (`Sources/FBDCore/Controllers/HotkeyController.swift`)
- [x] `Display.isVirtual` heuristic (magic IDs 0xF0F0/0x896/vendor 0x0100) — replace with authoritative source: track IDs created by VirtualScreenController and cross-check with display name prefix. DONE (cycle 4: Settings-persisted VirtualDisplayRegistry; heuristics kept as documented fallback).

### P2 (tests/coverage)
- [x] HTTP routing layer tests (AppCore.routeHTTP): needs the router extracted from the app target into a testable location; cover auth, 404/405/401, body parsing, virtual actions. DONE (cycle 5: HTTPRouter in FBDCore + 21 tests + live matrix).
- [x] EDIDController tests: override round-trip, auto-apply gating, restore. PARTIAL (cycle 2: validation + persistence gate tests; hardware-bound paths still need a real device).
- [ ] CombinedController property tests: property-style sweep of slider→hardware/XDR mapping (fast-check style, deterministic seed).
- [ ] Night Shift/True Tone controller tests on the fallback path (CBClient unavailable → graceful nil, no crash).

### P3 (polish/UX/perf)
- [ ] A11y pass on the NSPanel: VoiceOver labels for header buttons (x, gear), focus order, minimum text contrast on the gradient icon.
- [ ] Display model perf: group @Published into sub-state structs (identity/brightness/XDR/modes) to cut objectWillChange churn.
- [ ] `fbdcli modes <id>` shows current mode marker; add `--json` flag for list/info (scripting users).
- [ ] CHANGELOG.md + RELEASING.md (tag → build → notarize → appcast steps), version bump flow.

## Risks / Known Gaps

- macOS 27 beta: SLSDisplaySetPresetData write-protected (XDR native upscaling → software fallback), IOMobileFramebufferOpen entitlement-gated, VirtualDisplay.framework absent (SidecarCore path), True Tone degraded. Tracked in docs/PRIVATE_APIS.md; re-check per beta.
- CI runs Intel (macos-15): DDC mock tests run everywhere now, but no arm64 CI coverage without paid xlarge runner.
- HTTP server: hand-rolled HTTP/1.1 — keep-alive/chunked unsupported by design (Connection: close); clients must handle it (fbdcli does).
- Sparkle: feed URL empty → updates inert until a release exists (by design).
- Case-insensitive filesystem footgun: target directories must not be case-variants (FBDCLIParser precedent).
